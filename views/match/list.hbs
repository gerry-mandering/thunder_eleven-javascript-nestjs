<!DOCTYPE html>
<html>
<head>
  <title>Match List Board</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }

    h2 {
      text-align: center;
    }

    .match-list {
      max-width: 1200px;
      margin: 0 auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .match-list ul {
      list-style-type: none;
      padding: 0;
    }

    .match-list li {
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid #ccc;
      padding: 10px 0;
    }

    .match-list li:last-child {
      border-bottom: none;
    }

    .match-list li span {
      flex: 1;
    }

    .match-list .header {
      font-weight: bold;
    }
  </style>
</head>
<body>
<h2>Match List Board</h2>
<div class="match-list">
  <ul>
    <li class="header">
      <span>Stadium Name</span>
      <span>Match Date & Time</span>
      <span>Match Level</span>
      <span>Headcount Per Team</span>
      <span>Home Team</span>
      <span>Away Team</span>
    </li>
    {{#each matches}}
      <li>
        <span>{{ stadiumName }}</span>
        <span>{{ matchDateTime }}</span>
        <span>{{ matchLevel }}</span>
        <span>{{ headCountPerTeam }}</span>
        <span>{{ homeTeamName }}</span>
        <span>{{ awayTeamName }}</span>
      </li>
    {{/each}}
  </ul>
</div>
</body>
</html>
